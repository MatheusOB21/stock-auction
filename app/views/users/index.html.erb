<section id="users-regular">
  <div><h2>Usuários regulares cadastrado no sistema<h2><div>
  <% if @users_regular.any? %>
  </div>
    <table class="table table-bordered table-striped">
      <thead>
        <tr>
          <th scope="col"><%= User.human_attribute_name(:name) %></th>
          <th scope="col"><%= User.human_attribute_name(:email) %></th>
          <th scope="col"><%= User.human_attribute_name(:cpf) %></th>
        </tr>
      </thead>
      <tbody>
          <% @users_regular.each do |user|%>
            <tr id=<%= "#{user.id}"%> >
              <th scope="row"><%= user.name %></th>
              <td><%= user.email %></td>
              <td><%= user.cpf %></td>
              <% if user.block? %>
                <td><%= button_to "Desbloquear", user_blacklist_path(user.id, Blacklist.all.find_by(cpf: user.cpf)), method: :delete, class: 'btn btn-danger' %></td>
              <% else %>
                <td><%= button_to "Bloquear", user_blacklists_path(user.id), class: 'btn btn-danger' %></td>
              <% end %>
            </tr>
          <% end %>  
      </tbody>
    </table>
  </div>
  <% else %>
    <h3>Nenhum usuário regular cadastrado no sistema</h3>
  <% end %>
</section>